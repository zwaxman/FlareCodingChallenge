import axios from 'axios'

/**
 * ACTION TYPES
 */

/**
 * INITIAL STATE
 */
const initialState = []

/**
 * ACTION CREATORS
 */

/**
 * THUNK CREATORS
 */

/**
 * REDUCER
 */
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}